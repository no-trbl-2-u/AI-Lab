```yaml
phase: 3
claim: >
  This lab's decomposition procedure, run on a real task already partly
  split by hand, produces a compatible-or-better decomposition — and where
  it diverges, the divergence is explainable, not arbitrary.
cites: lab/README.md#long-horizon-runner
build: >
  lab/modular-skill-composition/runs/01-long-horizon-runner.md — the
  /decompose-task output against the long-horizon runner target, plus a
  side-by-side comparison against lab/README.md's existing five-part table
  (contract schema + checker, tick loop, external completion gate,
  fresh-context restart, capped escalation).
failing_check: >
  type: reading — this phase is a comprehension check, not a new build. The
  check is the comparison table itself: every divergence from the five-part
  table is named and either adopted or rejected with a stated reason.
done_when: >
  The comparison table has zero unexplained divergences, and the run is
  recorded in lab/modular-skill-composition/runs/ per PIPELINE.md stage 5.
```

## Status: queued

Blocked on phase 2. This is the dogfood step, and per PIPELINE.md's stage-5
verification loop, it is expected to surface where phases 1–2 were wrong —
that's this phase working as intended, not the lab failing.

## Why the long-horizon runner is the right target

It's the one large task in this repo that's already been decomposed by a
human, informally, into a five-part table
([lab/README.md](../README.md#long-horizon-runner)) — so this phase has a
ground truth to compare against instead of judging the output in a vacuum.
