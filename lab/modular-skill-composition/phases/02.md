```yaml
phase: 2
claim: >
  A large task can be split into skill specs that each pass phase 1's guard,
  using a stopping rule analogous to PIPELINE.md's phase rule: exactly one
  failing check and one responsibility sentence per skill.
cites: PIPELINE.md#stage-2--decompose-resource-gap
build: >
  .claude/skills/decompose-task/SKILL.md — a skill that interviews for a
  large task's shape, proposes a split, and writes each candidate to a
  skill-spec file conforming to workshop/spec/skill-contract.md, plus a
  composition manifest naming invocation order between them.
failing_check: >
  Run /decompose-task against a real task, then run
  `node guards/skill-contract.mjs <every produced spec>` — must exit 0 for
  all of them, and the composition manifest must contain no cycles.
done_when: >
  Every produced spec passes the guard on the first attempt (not after
  hand-fixing), and the manifest's dependency graph is acyclic.
```

## Status: queued

Blocked on phase 1 (done) — nothing to check specs against without the
contract and guard existing first.

## Open questions going in

- Whether the interview should ask "what's the failing check?" before or
  after proposing the split — PIPELINE.md's own stage 2 draws phases at
  comprehension seams using resource deltas as input; this phase has no
  equivalent delta to draw on, so the interview may need to manufacture one
  by asking what's already understood about the task before proposing units.
- Whether "exactly one failing check" is enforceable at proposal time or
  only after the fact — the guard checks the *shape* of the check, not
  whether it's the *right* one, per skill-contract.md's own stated limit.

Do not start this phase until these have real answers, per PIPELINE.md
stage 3's gate — if not, split further or run `/phase-overview` first.
