```yaml
phase: 1
claim: >
  A skill spec can be checked mechanically for modularity — single
  responsibility, explicit inputs/outputs, explicit composition, its own
  failing check — without a human judgment call.
cites: resources/05-agent-skills-progressive-disclosure.md#authoring-guidance
build: >
  workshop/spec/skill-contract.md (the contract) and guards/skill-contract.mjs
  (the checker), plus examples/good-skill.md and examples/bad-skill.md as
  fixtures.
failing_check: >
  node guards/skill-contract.mjs lab/modular-skill-composition/examples/bad-skill.md
done_when: >
  The command above exits non-zero and names every specific divergence
  (missing sections, a two-part responsibility, a placeholder failing check);
  the same command against examples/good-skill.md exits 0.
```

## Status: done

## What was built

- [`workshop/spec/skill-contract.md`](../../../workshop/spec/skill-contract.md)
  — the contract, in prose: required frontmatter, four required sections,
  the "and" rule for `## Responsibility`, and the `type: reading` exemption
  for `## Failing check` (mirrors PIPELINE.md's own reading-task exemption).
- [`guards/skill-contract.mjs`](../../../guards/skill-contract.mjs) — the
  checker, structured like `guards/intake.mjs`: a fail list, one entry per
  divergence, exit 1 with all of them printed, exit 0 silent on success.
- [`examples/bad-skill.md`](../examples/bad-skill.md) and
  [`examples/good-skill.md`](../examples/good-skill.md) — a spec that fails
  on five distinct grounds at once, and its split-and-fixed counterpart.

## Verified

```
$ node guards/skill-contract.mjs lab/modular-skill-composition/examples/bad-skill.md
skill-contract guard: 5 problem(s)
  - ...missing the required section "## Inputs"
  - ...missing the required section "## Outputs"
  - ...missing the required section "## Composes with"
  - ...reads as two responsibilities joined by "and"
  - ..."## Failing check" is empty or a placeholder
exit: 1

$ node guards/skill-contract.mjs lab/modular-skill-composition/examples/good-skill.md
exit: 0
```

## Divergences from the claim (per PIPELINE.md stage 5)

None yet — this is the first check of this phase's own output, not a 30-day
re-verification. Re-run stage 5 on this phase after 30 days, source closed,
and record the result here.
